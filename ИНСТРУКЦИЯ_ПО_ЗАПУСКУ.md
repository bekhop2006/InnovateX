# 🚀 Полная инструкция по запуску InnovateX

## 📋 Что нужно установить

Убедитесь, что у вас установлено:
- **Docker Desktop** (последняя версия)
- **Node.js** версии 18 или выше
- **Python** версии 3.10 или выше (если запускаете локально без Docker)

## 🎯 Быстрый старт (рекомендуется)

### Шаг 1: Запустить Backend и Базу данных через Docker

```bash
cd /Users/beknur/InnovateX

# Запустить backend и PostgreSQL
docker-compose up -d
```

Проверьте что всё запустилось:
```bash
docker-compose ps
```

Вы должны увидеть:
- `innovatex_postgres` - работает
- `innovatex_backend` - работает

### Шаг 2: Применить миграции базы данных

```bash
# Создать миграцию
docker-compose exec backend alembic revision --autogenerate -m "Add authentication and scan history"

# Применить миграцию
docker-compose exec backend alembic upgrade head
```

### Шаг 3: Создать первого администратора

```bash
docker-compose exec backend python scripts/create_admin.py
```

Будет создан админ:
- **Email:** admin@innovatex.com
- **Пароль:** admin123

### Шаг 4: Установить зависимости для Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Установить React Router
npm install react-router-dom
```

### Шаг 5: Запустить Frontend

```bash
# Находясь в папке frontend
npm run dev
```

### Шаг 6: Открыть приложение

Откройте браузер и перейдите по адресу:
- **Frontend:** http://localhost:5173
- **Backend API Docs:** http://localhost:8000/docs

## ✅ Готово! Теперь можно использовать систему

1. **Зарегистрируйтесь** на главной странице
2. **Войдите** в систему
3. **Загрузите PDF** для сканирования
4. **Просмотрите историю** сканов в дашборде

---

## 🔧 Альтернативный запуск (без Docker для backend)

Если хотите запустить backend локально без Docker:

### 1. Установить PostgreSQL локально

```bash
# На macOS через Homebrew
brew install postgresql@15
brew services start postgresql@15

# Создать базу данных
createdb innovatex_db
```

### 2. Настроить переменные окружения

Создайте файл `.env` в папке `backend`:

```env
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/innovatex_db
SECRET_KEY=your-super-secret-key-change-this
APP_ENV=development
DEBUG=True
```

Сгенерируйте безопасный SECRET_KEY:
```bash
openssl rand -hex 32
```

### 3. Установить зависимости Python

```bash
cd backend

# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 4. Применить миграции

```bash
cd backend

# Создать миграцию
alembic revision --autogenerate -m "Add authentication and scan history"

# Применить
alembic upgrade head
```

### 5. Создать администратора

```bash
cd backend
python scripts/create_admin.py
```

### 6. Запустить backend

```bash
cd backend
python main.py
```

Backend будет доступен на http://localhost:8000

---

## 🧪 Тестирование системы

### Тест 1: Регистрация нового пользователя

```bash
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тест",
    "email": "test@example.com",
    "password": "password123"
  }'
```

### Тест 2: Вход в систему

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'
```

Сохраните `access_token` из ответа!

### Тест 3: Получить текущего пользователя

```bash
curl -X GET http://localhost:8000/api/auth/me \
  -H "Authorization: Bearer ВАШ_ТОКЕН_ЗДЕСЬ"
```

### Тест 4: Сканировать документ

```bash
curl -X POST "http://localhost:8000/api/document-inspector/detect?conf_threshold=0.5" \
  -H "Authorization: Bearer ВАШ_ТОКЕН_ЗДЕСЬ" \
  -F "file=@/путь/к/вашему/документу.pdf"
```

### Тест 5: Посмотреть историю сканов

```bash
curl -X GET http://localhost:8000/api/scans/ \
  -H "Authorization: Bearer ВАШ_ТОКЕН_ЗДЕСЬ"
```

---

## 📊 Доступ к интерфейсам

После успешного запуска доступны:

| Сервис | URL | Описание |
|--------|-----|----------|
| Frontend | http://localhost:5173 | Основное приложение |
| Backend API | http://localhost:8000 | REST API |
| API Документация | http://localhost:8000/docs | Swagger UI |
| ReDoc | http://localhost:8000/redoc | Альтернативная документация |
| PgAdmin | http://localhost:5050 | Управление БД (если запущено) |

### PgAdmin (опционально)

Для доступа к базе данных через веб-интерфейс:

```bash
docker-compose --profile tools up -d
```

Откройте http://localhost:5050:
- **Email:** admin@innovatex.com
- **Пароль:** admin

---

## 🎨 Структура приложения после запуска

```
Приложение
├── Главная страница (/)
│   └── Редирект на /auth или /dashboard
│
├── Авторизация (/auth)
│   ├── Вход в систему
│   └── Регистрация
│
└── Дашборд (/dashboard) [требует авторизации]
    ├── Новое сканирование (/dashboard/scan)
    ├── История сканов (/dashboard/history)
    ├── Профиль (/dashboard/profile)
    └── Админ панель (/dashboard/admin) [только для админа]
```

---

## 🔐 Учётные данные по умолчанию

### Администратор
- **Email:** admin@innovatex.com
- **Пароль:** admin123
- **Роль:** admin

⚠️ **ВАЖНО:** Смените пароль администратора после первого входа!

### Тестовый пользователь
Создайте через форму регистрации на сайте.

---

## 🛠️ Полезные команды

### Docker команды

```bash
# Просмотр логов
docker-compose logs -f backend
docker-compose logs -f postgres

# Перезапуск сервисов
docker-compose restart backend

# Остановить всё
docker-compose down

# Остановить и удалить данные
docker-compose down -v

# Пересобрать и запустить
docker-compose up -d --build
```

### Миграции базы данных

```bash
# Создать новую миграцию
docker-compose exec backend alembic revision --autogenerate -m "Описание изменений"

# Применить миграции
docker-compose exec backend alembic upgrade head

# Откатить одну миграцию назад
docker-compose exec backend alembic downgrade -1

# Посмотреть текущую версию
docker-compose exec backend alembic current

# Посмотреть историю миграций
docker-compose exec backend alembic history
```

### Управление пользователями

```bash
# Создать администратора
docker-compose exec backend python scripts/create_admin.py

# С кастомными данными
docker-compose exec backend python scripts/create_admin.py \
  --email myemail@example.com \
  --password mypassword123 \
  --name "Имя" \
  --surname "Фамилия"
```

### Frontend команды

```bash
cd frontend

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev

# Собрать для продакшена
npm run build

# Предпросмотр сборки
npm run preview
```

---

## 🐛 Решение проблем

### Проблема: Docker не запускается

**Решение:**
```bash
# Проверьте что Docker Desktop запущен
docker ps

# Перезапустите Docker Desktop
# Затем попробуйте снова
docker-compose up -d
```

### Проблема: Порт 5432 уже занят

**Решение:**
Либо остановите локальный PostgreSQL:
```bash
brew services stop postgresql@15
```

Либо измените порт в `docker-compose.yml`:
```yaml
ports:
  - "5434:5432"  # Изменить с 5433 на 5434
```

### Проблема: Backend не видит модель

**Решение:**
```bash
# Пересобрать backend контейнер
docker-compose up -d --build backend

# Применить миграции
docker-compose exec backend alembic upgrade head
```

### Проблема: Frontend не может подключиться к Backend

**Решение:**
1. Проверьте что backend работает: http://localhost:8000/health
2. Проверьте `frontend/vite.config.js` - должен быть правильный proxy
3. Очистите кеш браузера
4. Перезапустите frontend: `npm run dev`

### Проблема: Ошибка 401 Unauthorized

**Решение:**
1. Проверьте что токен действителен (срок - 24 часа)
2. Очистите localStorage в браузере
3. Войдите заново
4. Используйте endpoint `/api/auth/refresh` для обновления токена

### Проблема: Миграции не применяются

**Решение:**
```bash
# Проверить статус
docker-compose exec backend alembic current

# Если база пустая, применить миграции
docker-compose exec backend alembic upgrade head

# Если есть конфликты, сбросить всё (ОСТОРОЖНО!)
docker-compose down -v
docker-compose up -d
docker-compose exec backend alembic upgrade head
```

---

## 📦 Что включено в систему

### ✅ Реализованные функции

**Аутентификация:**
- ✅ Регистрация пользователей
- ✅ Вход по email и паролю
- ✅ JWT токены с автоматическим обновлением
- ✅ Роли пользователей (admin/user)
- ✅ Защищённые маршруты

**Сканирование документов:**
- ✅ Загрузка и анализ PDF
- ✅ Определение QR кодов, подписей, печатей
- ✅ Настраиваемый порог уверенности
- ✅ Автоматическое сохранение в историю

**История сканов:**
- ✅ Просмотр всех своих сканов
- ✅ Детальная информация о каждом скане
- ✅ Скачивание оригинальных PDF
- ✅ Удаление сканов
- ✅ Статистика по сканам
- ✅ Автоматическое удаление через 90 дней

**Админ панель:**
- ✅ Просмотр всех пользователей
- ✅ Просмотр сканов любого пользователя
- ✅ Статистика по всей системе
- ✅ Управление пользователями

---

## 🎓 Дополнительная информация

### Документация API

После запуска backend, перейдите на http://localhost:8000/docs для интерактивной документации API.

### Структура проекта

Смотрите файл `PROJECT_STRUCTURE.md` для детального описания структуры проекта.

### Статус реализации

Смотрите файл `IMPLEMENTATION_STATUS.md` для полного списка реализованных функций.

### Интеграция компонентов

Смотрите файл `FINAL_INTEGRATION_GUIDE.md` для детальной информации по интеграции.

---

## 📞 Помощь и поддержка

Если возникли проблемы:

1. Проверьте логи: `docker-compose logs -f`
2. Просмотрите документацию API: http://localhost:8000/docs
3. Убедитесь что все порты свободны: 8000, 5173, 5432
4. Проверьте что Docker Desktop запущен

---

## 🚀 Готово к работе!

После выполнения всех шагов у вас будет:

- ✅ Работающий backend на порту 8000
- ✅ Работающий frontend на порту 5173
- ✅ База данных PostgreSQL
- ✅ Настроенная аутентификация
- ✅ Система хранения истории сканов
- ✅ Админ панель для управления

**Приятного использования!** 🎉

