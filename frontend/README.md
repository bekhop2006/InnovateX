# 🎨 Frontend - Construction PDF Analyzer

React-приложение для анализа строительных документов (PDF) с использованием YOLOv8.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
cd frontend
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для production

```bash
npm run build
npm run preview  # Предпросмотр production сборки
```

## 🏗️ Технологии

- **React 18.2** - UI библиотека
- **Vite 5.0** - Build tool и dev server
- **React-PDF 7.5** - Рендеринг PDF документов
- **SASS** - CSS препроцессор
- **PDF.js** - PDF обработка

## 📁 Структура проекта

```
frontend/
├── src/
│   ├── main.jsx              # Точка входа
│   ├── App.jsx               # Главный компонент
│   ├── App.scss              # Глобальные стили
│   └── components/
│       ├── UploadCard.jsx    # Компонент загрузки
│       ├── PdfViewer.jsx     # Просмотр PDF с аннотациями
│       └── ResultsPanel.jsx  # Панель результатов
├── index.html
├── vite.config.js            # Конфигурация Vite
├── package.json
└── example.json              # Пример данных для fallback
```

## 🔌 Интеграция с Backend

Frontend подключается к backend API:

- **Base URL**: `http://localhost:8000`
- **Endpoint**: `POST /api/document-inspector/detect`
- **Parameters**: `conf_threshold` (0.0-1.0, по умолчанию 0.5)

### Формат запроса

```javascript
const form = new FormData()
form.append('file', pdfFile)

const response = await fetch(
  'http://localhost:8000/api/document-inspector/detect?conf_threshold=0.5',
  {
    method: 'POST',
    body: form,
  }
)
```

### Формат ответа

```json
{
  "document_name": "example.pdf",
  "total_pages": 2,
  "pages": [
    {
      "page_number": 1,
      "page_size": { "width": 2380, "height": 3368 },
      "annotations": [
        {
          "id": "detection_1",
          "category": "signature",
          "bbox": { "x": 100, "y": 200, "width": 50, "height": 30 },
          "confidence": 0.94
        }
      ]
    }
  ],
  "processing_time": 2.5
}
```

## 🎨 Функционал

### 1. Загрузка документов
- ✅ Drag & drop
- ✅ Клик для выбора файла
- ✅ Валидация формата (только PDF)
- ✅ Индикация загрузки

### 2. Просмотр результатов
- ✅ Статистика найденных элементов
- ✅ Время обработки
- ✅ Легенда категорий

### 3. Интерактивный просмотр PDF
- ✅ Навигация по страницам
- ✅ Отображение bounding boxes
- ✅ Фильтры по категориям (QR, подписи, печати)
- ✅ Миниатюры с найденными элементами
- ✅ Spotlight эффект при выборе
- ✅ Отображение confidence в tooltip

### Категории и цвета
- 🔵 **Blue** - QR коды
- 🟢 **Green** - Подписи (signatures)
- 🟠 **Orange** - Печати (stamps)

## ⚙️ Конфигурация

### Vite прокси

В `vite.config.js` настроен прокси для API:

```javascript
server: {
  proxy: {
    '/api': {
      target: 'http://localhost:8000',
      changeOrigin: true,
    }
  }
}
```

### Изменение порта

По умолчанию приложение запускается на порту **5173**. Для изменения:

```javascript
// vite.config.js
server: {
  port: 3000, // Ваш порт
}
```

## 🧪 Тестирование

### Режим fallback

Если backend недоступен, приложение автоматически загружает `example.json` с демо-данными.

### Тестирование с backend

1. Запустите backend:
   ```bash
   cd ../
   docker-compose up -d
   ```

2. Убедитесь что backend доступен:
   ```bash
   curl http://localhost:8000/api/document-inspector/health
   ```

3. Запустите frontend:
   ```bash
   cd frontend
   npm run dev
   ```

4. Откройте http://localhost:5173 и загрузите PDF

## 🐳 Docker

Frontend можно запустить в Docker (см. docker-compose.yml в корне проекта).

## 🎯 Скрипты

```bash
npm run dev      # Запуск dev сервера
npm run build    # Production сборка
npm run preview  # Предпросмотр production
```

## 🔧 Troubleshooting

### Ошибка CORS

Если возникают CORS ошибки, убедитесь что:
1. Backend запущен на порту 8000
2. В backend разрешены CORS запросы от localhost:5173
3. Используется прокси в vite.config.js

### Backend недоступен

Проверьте:
```bash
curl http://localhost:8000/health
curl http://localhost:8000/api/document-inspector/health
```

### PDF не отображается

Убедитесь что установлена последняя версия react-pdf:
```bash
npm install react-pdf@latest
```

## 📝 Лицензия

MIT

---

**InnovateX Team** | 2025

