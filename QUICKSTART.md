# 🚀 Quick Start Guide - Document Inspector

Быстрое руководство по запуску проекта за 5 минут.

## ⚡ Шаг 1: Установка (2 минуты)

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/InnovateX.git
cd InnovateX

# Перейти в backend
cd backend

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Mac/Linux
# или venv\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt
```

## 📊 Шаг 2: Подготовка датасета (2 минуты)

```bash
cd services/document_inspector
python prepare_dataset.py
```

**Что происходит:**
- Конвертация PDF → изображения
- Создание YOLO аннотаций
- Разделение на train/val (80/20)

**Результат:** `dataset/yolo_dataset/` готов к обучению

## 🤖 Шаг 3: Обучение модели (15-30 минут)

```bash
python train_model.py
```

**Параметры по умолчанию:**
- Модель: YOLOv8-nano (быстрая)
- Эпохи: 100
- Batch: 16

**Ускорение обучения:**
- Уменьшите эпохи до 50 для быстрого теста
- Используйте GPU (CUDA) если доступен

**Результат:** `models/document_inspector_yolo.pt`

## 🌐 Шаг 4: Запуск API (30 секунд)

```bash
# Вернуться в backend
cd ../../..

# Запустить сервер
python main.py
```

Сервер запущен на `http://localhost:8000`

📚 API Docs: `http://localhost:8000/docs`

## 🧪 Шаг 5: Тестирование (1 минута)

### Вариант A: Через браузер

1. Открыть `http://localhost:8000/docs`
2. Найти `/api/document-inspector/detect`
3. Нажать "Try it out"
4. Загрузить PDF
5. Нажать "Execute"

### Вариант B: Через curl

```bash
curl -X POST "http://localhost:8000/api/document-inspector/detect" \
  -F "file=@/path/to/document.pdf" \
  -o result.json
```

### Вариант C: Через Python

```python
import requests

with open("document.pdf", "rb") as f:
    response = requests.post(
        "http://localhost:8000/api/document-inspector/detect",
        files={"file": f}
    )
    print(response.json())
```

### Вариант D: Тестовый скрипт

```bash
cd backend/services/document_inspector
python test_api.py
```

## 📸 Получить визуализацию

```bash
curl -X POST "http://localhost:8000/api/document-inspector/detect-visualize?page_number=1" \
  -F "file=@document.pdf" \
  --output result.png
```

## 🎯 Что дальше?

### Улучшение точности
- Увеличьте epochs до 200
- Используйте `yolov8s.pt` (small) вместо nano
- Добавьте больше данных для обучения

### Продакшн деплой
- Настройте CORS в `backend/main.py`
- Используйте Gunicorn/Nginx
- Контейнеризация с Docker

### Интеграция
- Импортируйте детектор в ваш код
- Используйте REST API из любого языка
- Batch обработка документов

## ❓ Troubleshooting

**Проблема:** Model not loaded
```bash
# Решение: Проверить путь к модели
ls models/document_inspector_yolo.pt
# Если нет - обучить модель заново
```

**Проблема:** Out of memory
```python
# Решение: В train_model.py уменьшить BATCH
BATCH = 8  # Вместо 16
```

**Проблема:** Низкая точность
```python
# Решение 1: Больше эпох
EPOCHS = 200

# Решение 2: Лучшая модель
MODEL_NAME = "yolov8s.pt"
```

## 📝 Полная документация

См. [README.md](README.md) для детальной документации.

---

**Время до первого результата:** ~20 минут (без обучения) или ~45 минут (с обучением)

Удачи! 🚀

